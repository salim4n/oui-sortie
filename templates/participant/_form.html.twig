{{ form_start(form) }}

    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.telephone) }}
    {{ form_row(form.pseudo) }}
    {%if app.user and is_granted('ROLE_ADMIN') %}
        {{ form_row(form.actif) }}
        <select name="campus" >
            {% for campus in campuss %}
                <option value="{{ campus.id }}">{{ campus.nom }}</option>
            {%endfor %}
        </select><a href="{{ path('app_campus_new')}}">show</a><br>
        <labe>admin</labe>
        <input type="checkbox" name="is_admin"  value="true" ><br>
    {% else %}
        {% do form.actif.setRendered %}
    {% endif %}
<button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
